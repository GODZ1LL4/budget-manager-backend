# Node modules (no subir dependencias)
node_modules/

# Archivos de entorno sensibles (claves, URLs)
.env
.env.local
.env.development
.env.production

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Archivos del sistema
.DS_Store
Thumbs.db

# Salida de build (si llegas a usar Babel o TS en el futuro)
dist/
build/
